{
  "name": "@one-resume/content",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Content JSON exporter. Parses CV / projects markdown via @one-resume/parser and writes the per-language content.json the pro-landing site consumes.",
  "engines": {
    "node": ">=22.0.0"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@one-resume/types": "*",
    "@one-resume/parser": "*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.0"
  },
  "scripts": {
    "test": "node --import tsx --test \"test/**/*.test.ts\""
  }
}