{
  "name": "@one-resume/localization",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Locale name-parsing (detectLocale) and the en/it label values for the per-tool PdfLabels / DocxLabels shapes. Renderers stay language-agnostic; the values live here.",
  "engines": {
    "node": ">=22.0.0"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@one-resume/pdf": "*",
    "@one-resume/docx": "*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.0"
  },
  "scripts": {
    "test": "node --import tsx --test \"test/**/*.test.ts\""
  }
}
