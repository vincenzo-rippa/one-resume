{
  "name": "cv-parser",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic markdown parser for CV content (content/cv/*.md → ParsedCv JSON).",
  "engines": {
    "node": ">=22.0.0"
  },
  "bin": {
    "cv-parse": "bin/cli.ts"
  },
  "scripts": {
    "test": "node --import tsx --test \"test/**/*.test.ts\"",
    "parse": "node --import tsx bin/cli.ts"
  },
  "dependencies": {
    "marked": "^14.1.4",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.0"
  }
}
