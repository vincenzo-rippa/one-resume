# dependencies
node_modules/

# generated build output (PDFs / DOCX / content.json — regenerated from source)
/out/

# Claude private data
.claude/

# typst temp cache
**/.cache/

# env: local overrides only — .env.example is committed
.env
.env.*.local

# apps/special is its own private repo, vendored locally — never part of this one
apps/special/

# misc
.DS_Store
*.tsbuildinfo
.rest